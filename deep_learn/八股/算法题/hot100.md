
## 我会怎么带你刷每一道题

**1. 一句话读懂题目**  
先把题目翻译成人话：输入是什么、输出是什么、到底要我们找什么。

**2. 题眼识别**  
告诉你看到哪些关键词要联想到哪些套路，比如：

| 题目信号                  | 常见套路     |
| --------------------- | -------- |
| “两数之和 / 是否存在”         | 哈希表      |
| “连续子数组 / 最长 / 最短”     | 滑动窗口、前缀和 |
| “有序数组 / 第 k 个 / 最小最大” | 二分       |
| “括号 / 最近更大更小”         | 栈、单调栈    |
| “链表第 n 个 / 环 / 相交”    | 快慢指针、双指针 |
| “所有组合 / 所有路径”         | 回溯       |
| “树的层级 / 最短路径”         | BFS      |
| “最大收益 / 子序列 / 打家劫舍”   | 动态规划     |

**3. 从暴力解到最优解**  
不是直接甩代码，而是讲清楚：  
为什么暴力会超时？  
瓶颈在哪里？  
怎么用数据结构或算法思想优化？

**4. 面试表达版思路**  
我会给你一段你在面试里可以直接说的话，让你不是“会写但讲不出来”。

**5. 代码题解**  
默认用 **Python** 写，注释会清楚。你要 Java、C++、Go 也可以直接说。

**6. 复杂度分析**  
时间复杂度、空间复杂度，以及为什么是这个复杂度。

**7. 记忆方法**  
每题给你一个“题眼口诀”或“模式卡片”，比如：

> 两数之和：  
> 目标差值先想到哈希表。  
> 一边遍历，一边问：之前有没有人能和我凑 target？

**8. 举一反三**  
告诉你这题能迁移到哪些题，比如“两数之和”会连接到“三数之和”“四数之和”“两数之和 II”“子数组和为 K”。

LeetCode 官方的 Hot 100 / Top 100 Liked 本身就是按热门面试题组织的经典题单，适合短时间建立算法基本功。([力扣 LeetCode](https://leetcode.cn/problem-list/2cktkvj/?utm_source=chatgpt.com "🔥 LeetCode 热题HOT 100 - 力扣（LeetCode）全球极客挚爱 ...")) 学习方法上，我会帮你做“主动回忆 + 间隔复习 + 混合练习”：比起看完题解觉得懂了，更重要的是隔一段时间能自己重新推出来；学习科学里也强调分散练习和主动提取更有利于长期保持与迁移。([psychology.ucsd.edu](https://psychology.ucsd.edu/undergraduate-program/undergraduate-resources/academic-writing-resources/effective-studying/spaced-practice.html?utm_source=chatgpt.com "Spaced Practice"))

## 我建议你每题按这个节奏练

第一次：先自己想 10 分钟，不会也没关系。  
第二步：看我的讲解，重点看“为什么想到这个套路”。  
第三步：关掉题解，自己手写一遍。  
第四步：第二天再写一遍。  
第五步：一周后只看题名，口述思路 + 写核心代码。

Hot100 不要追求“刷过”，要追求看到题能立刻分类：

> 这题是什么类型？  
> 核心状态/指针/数据结构是什么？  
> 有什么坑？  
> 有没有模板？

之后你直接把第一道题发来就行。  
我会按“新手能懂、面试能讲、复习能记、变形能做”的标准来讲。





# 2、字母异位词分组
![[Pasted image 20260131144041.png]]


用到了字典哈希。
先遍历对所有单词进行排序，然后以排序后的词作为键，值是一个列表，然后判断字典中是否有这个键，然后将其添加到这个排序后的键对应的值（列表）中。这样所有的异位词组都会存到这个键对应的列表中。只需要取值，然后进行list就可以了。

代码
```python
class Solution:

    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        mp = collections.defaultdict(list)
        for st in strs:

            key = "".join(sorted(st))

            mp[key].append(st)

        return list(mp.values())
```

在代码中用到了一个叫collections的库，这个库可以帮助我少点检查是否有这个键的操作。直接向里面添加，如果有就可以对这个键对应的值进行操作，如果没有，会新建一个这个键，直接进行操作。

defaultdict（list）这个括号中填写一个数据类型。一旦检测到没有这个键，新建的这个键对应的值的数据类型就是这个默认的数据类型。
soted 就是进行排序。不管扔给他的是字符串还是列表，其返回都是一个列表。




最快的排序的复杂度是O（nlogn）
hashset的查找速度是O（1）。直接执行以此查询就行。


什么是hashset。就是一个只有键，没有值得哈希表。（可以用来执行自动去重）
并且无序无索引。就像一个大麻袋，里面有很多数字，你只能问里面有没有这个数字。但是你不能问这个里面第一个的数字是多少。

hashmap字典，hashset是集合。


# 3、最长连续序列

![[Pasted image 20260201133705.png]]


```python
class Solution:

    def longestConsecutive(self, nums: List[int]) -> int:

        # 1. 如果数组是空的，直接返回 0

        if not nums:

            return 0

        # 2. 把列表转成集合 (Set)

        # 这一步是为了让查找操作变成 O(1)，同时去重

        num_set = set(nums)

        longest_streak = 0

        # 3. 遍历集合中的每个数

        for num in num_set:

            # 4. 核心判断：只有当 num 是“龙头”时，才开始数数

            # 如果 num-1 存在，说明 num 肯定在以 num-1 (或更小) 开头的序列里

            # 所以我们直接跳过，不需要重复计算

            if (num - 1) not in num_set:

                current_num = num

                current_streak = 1

                # 5. 如果是龙头，就一直往后找 num+1, num+2...

                while (current_num + 1) in num_set:

                    current_num += 1

                    current_streak += 1

                # 6. 更新最大长度

                longest_streak = max(longest_streak, current_streak)

        return longest_streak

```


先一个循环，找到其是不是这个连续序列得头。（也就是你前面的数字存在不存在，如果不存在，你就是头，就可以数你后面有几个连续数字了。）


set的常用操作。

```
# 注意！空集合不能用 {}，那是空字典。必须用 set()
s = set()       
s = {1, 2, 3}   # 有初始值可以用花括号

添加
s.add(4)

删除
s.remove(4)

查找
if 4 in s:  # 这个操作是 O(1) 的，超级快！
    print("在里面")
```



# 4、移动0
这是一个关于快慢指针的题目。

![[Pasted image 20260201134457.png]]



直觉解法：遇到0就给它移动到最后。（计算复杂度太高，o（n2））

```python
# 你的思路的代码实现（虽然能跑，但效率低）
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        i = 0
        loop_count = len(nums)
        
        # 我们得手动控制循环次数，不然死循环
        processed = 0 
        while processed < loop_count:
            if nums[i] == 0:
                # 1. 把 0 拿出来 (这一步很慢！O(N))
                nums.pop(i)
                # 2. 加到最后
                nums.append(0)
                # 3. 注意！因为删了一个元素，后面的补上来了
                # 所以 i 不需要 +1，我们要检查补上来的这个新元素
            else:
                # 只有不是 0 的时候，指针才往后走
                i += 1
            
            processed += 1

```

双指针法

```python
class Solution:

    def moveZeroes(self, nums: List[int]) -> None:

        """

        Do not return anything, modify nums in-place instead.

        """

        # slow 指针：指向下一个存放“非零元素”的位置

        slow = 0

        # fast 指针：负责遍历数组寻找“非零元素”

        for fast in range(len(nums)):

            # 只有当找到非零元素时，才做事

            if nums[fast] != 0:

                # 把找到的这个非零元素，和 slow 位置的元素交换

                # 这样非零的就被挪前面了，0 就被换后面去了

                nums[slow], nums[fast] = nums[fast], nums[slow]

                # 既然这个位置坐好了人，slow 就往后移一位，准备迎接下一个

                slow += 1
```

这是一个关于快慢指针的题目。在这个题中有两个下标，一个下标只负责查看是不是0.如果不是0，就移动到前面（慢指针指向的位置，相当于快慢指针指向的数字交换位置）。如果是0就不动。

复杂度为O（n）。原地进行操作。
也不能移动O到最后，交换位置。这样会导致非0元素的原始顺序会发生变化。


# 5、盛水最多的容器
![[Pasted image 20260201140345.png]]


题目分析，从左右头开始扫描。必须淘汰矮的一边，因为只要矮的呢一边存在。不管另一边移动后下一个是多少，都没有用，因为这个矮的就是限制的最大瓶颈。

```python
class Solution:
    def maxArea(self, height: List[int]) -> int:
        # 1. 左右护法就位
        left = 0
        right = len(height) - 1
        
        max_water = 0
        
        # 2. 只要没撞上，就一直PK
        while left < right:
            # 计算当前宽度
            width = right - left
            
            # 计算当前水位（取决于短板）
            # 这一步通过比较，顺便决定了谁该被淘汰
            if height[left] < height[right]:
                current_height = height[left]
                # 左边矮，记录下当前面积，然后淘汰左边
                max_water = max(max_water, width * current_height)
                left += 1
            else:
                current_height = height[right]
                # 右边矮（或一样高），记录下当前面积，然后淘汰右边
                max_water = max(max_water, width * current_height)
                right -= 1
                
        return max_water
```

两个指针分别指向左右，然后进行移动，只需要对数据扫描一遍，就可以找到最多的盛水容器。



# 6、三数之和


![[Pasted image 20260201142238.png]]




这是一个双指针的题目
```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        # 1. 如果还没开始这就少于3个人，直接解散
        if not nums or len(nums) < 3:
            return []
        
        # 2. 关键第一步：排序！
        # 只有排序了，才能用双指针，才能方便去重
        nums.sort()
        
        result = []
        n = len(nums)
        
        # 3. 遍历每一个“老大”
        for i in range(n):
            # 如果老大自己都大于0，那后面肯定都大于0（因为排过序了）
            # 三个大于0的数加起来不可能等于0，直接结束
            if nums[i] > 0:
                break
                
            # 4. 老大去重：如果这个数和上一个数一样，跳过
            # i > 0 是为了保证 i-1 有效
            if i > 0 and nums[i] == nums[i-1]:
                continue
                
            # 5. 定义左右护法（双指针）
            # L 在老大右边第一个，R 在最后
            L = i + 1
            R = n - 1
            
            while L < R:
                total = nums[i] + nums[L] + nums[R]
                
                if total == 0:
                    # 找到了一组！
                    result.append([nums[i], nums[L], nums[R]])
                    
                    # 6. 护法去重（关键细节！）
                    # 比如 [-1, 0, 0, 0, 1]，找到一个0后，后面两个0都要跳过
                    while L < R and nums[L] == nums[L+1]:
                        L += 1
                    while L < R and nums[R] == nums[R-1]:
                        R -= 1
                    
                    # 找到答案后，两人同时收缩，找下一组
                    L += 1
                    R -= 1
                    
                elif total < 0:
                    # 结果太小了，把左边的（小的）变大一点
                    L += 1
                else:
                    # 结果太大了，把右边的（大的）变小一点
                    R -= 1
                    
        return result
```


首先对这个列表进行排序，从小到大排序。
然后进行循环，如果这个数跟左右两个指针指向的数的和为0，那么就是找到了一组。
需要注意的是：左右指针相遇就是两个指针已经对整个列表进行了扫描，已经寻找完毕。
循环时，这个数和上一个数不能相同。相同的话会找到同样的三个数。（去重）
左右指针移动的时候也要去重。


# 7、接雨水（题目较困难，多做）

![[Pasted image 20260201150207.png]]




接雨水问题。
```python
class Solution:

    def trap(self, height: List[int]) -> int:

        if not height:

            return 0

        # 1. 定义双指针

        left = 0

        right = len(height) - 1

        # 2. 定义左右两边的“墙”的高度

        left_max = 0

        right_max = 0

        water = 0

        # 3. 往中间逼近

        while left < right:

            # 更新左右两边的最高墙

            left_max = max(left_max, height[left])

            right_max = max(right_max, height[right])

            # 4. 核心逻辑：谁矮算谁

            if left_max < right_max:

                # 左边墙矮，说明左边这个位置的水位由 left_max 决定

                # 累加水量：瓶颈高度 - 柱子本身高度

                water += left_max - height[left]

                left += 1

            else:

                # 右边墙矮，说明右边这个位置的水位由 right_max 决定

                water += right_max - height[right]

                right -= 1

        return water

```


这个位置能接到多少雨水，取决于左右两边最高的墙中的较矮的一个。
只有当确定了，哪一边是短板的时候，才移动哪一边。

# 8、无重复字符的最长子串
![[Pasted image 20260201164831.png]]

```python

class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        # 1. 也是用 Set 做“记账本”，查重速度快 O(1)
        window_chars = set()
        
        # 2. 定义左指针
        left = 0
        max_len = 0
        
        # 3. 右指针主动去探索 (0 到 结束)
        for right in range(len(s)):
            current_char = s[right]
            
            # 4. 如果发现新来的字符已经在窗口里了（撞车了！）
            # 就要不断缩小左边，直到把那个重复的字符“挤出去”
            while current_char in window_chars:
                # 移除最左边的字符
                window_chars.remove(s[left])
                # 左指针向右缩
                left += 1
            
            # 5. 现在安全了，把新字符加进来
            window_chars.add(current_char)
            
            # 6. 量一下现在的宽度，挑战一下历史纪录
            # 宽度计算公式：right - left + 1
            max_len = max(max_len, right - left + 1)
            
        return max_len
```


在这个题目中，维护了两个指针，两个指针合在一起，相当于是一个滑动的窗口，一开始只移动右窗口，添加新的元素。一旦发现添加的元素中有已经含有的元素。则不断移动左窗口，知道把重复的旧元素排除出去。这个过程中，时刻计算窗口的长度，右指针减去左指针加一，并于已知的最大窗口进行比较，遍历一遍，即可得到最大的窗口。



# 9、找到字符串中的所有字母异位词
![[Pasted image 20260201165945.png]]



这个题目还是滑动窗口的题目。

```python
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        n, m = len(s), len(p)
        
        # 1. 边界条件：如果 s 比 p 还短，那肯定没有，直接回空
        if n < m:
            return []
        
        # 2. 准备两个 DNA 记录表 (长度26的数组，初始都是0)
        p_count = [0] * 26
        window_count = [0] * 26
        
        # 3. 先把 p 的 DNA 录入，同时把 s 的前 m 个字符（第一个窗口）录入
        for i in range(m):
            p_count[ord(p[i]) - ord('a')] += 1
            window_count[ord(s[i]) - ord('a')] += 1
            
        res = []
        
        # 4. 检查第一个窗口是否匹配
        if p_count == window_count:
            res.append(0)
            
        # 5. 开始滑动！从第 m 个字符开始，一直滑到最后
        # i 代表当前窗口【新加入】的那个字符的索引
        for i in range(m, n):
            # A. 进货：把新字符加入窗口
            new_char = s[i]
            window_count[ord(new_char) - ord('a')] += 1
            
            # B. 出货：把最左边被抛弃的字符移出窗口
            # 最左边的字符索引是 i - m
            old_char = s[i - m]
            window_count[ord(old_char) - ord('a')] -= 1
            
            # C. 比对：看看现在的窗口和 p 是否一样
            # 在 Python 里，比较两个列表的内容是否相同，直接用 == 就行，效率很高
            if window_count == p_count:
                # 记录起始索引 (就是 i - m + 1)
                res.append(i - m + 1)
                
        return res

```

在这个题目中，就是制作了一个和字串一样大小的窗口。然后制作了一个长为26的列表，然后默认第一位置的值就是a出现的次数。第二个位置的值就是b出现的次数，
 两个列表完全一样的时候，就说明这个符合添加，字串找到。记录开始的索引。 
 
 通过ord（）函数，就可以求出对应字符的ascll码。


# 10、和为K的子数组
![[Pasted image 20260201171413.png]]



看到题的第一反应是使用滑动窗口来做。但是不可以，因为有负的值。

一旦遇到负值，滑动窗口的指针就不知道移动哪一个。


```python
from collections import defaultdict

class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        # 1. 记账本：记录 {前缀和 : 出现的次数}
        # 核心细节：必须预设 {0: 1}
        # 含义是：前缀和为 0 的情况已经出现过 1 次了（就是刚开始还没动的时候）
        prefix_map = defaultdict(int)
        prefix_map[0] = 1
        
        current_sum = 0
        count = 0
        
        # 2. 遍历数组
        for num in nums:
            # 计算当前走到这里的累加和
            current_sum += num
            
            # 3. 核心公式查询：
            # 想知道有没有一个子数组和是 k，
            # 就看以前有没有出现过前缀和等于 (current_sum - k)
            target = current_sum - k
            
            # 如果以前出现过 target，说明中间这一段的和刚好就是 k
            if target in prefix_map:
                count += prefix_map[target]
            
            # 4. 把当前的前缀和记录到账本里
            prefix_map[current_sum] += 1
            
        return count

```


在这个题目中，建立了一个字典，key为前缀和，V为这个前缀和出现了几次。（可以观察到，如果值全都是正值，那么前缀和就是单调的，在这个字典中所有的值对应的都是1.）正是因为有负值的存在，所以导致前缀和的value不全为1.

一直求前缀和，找出当前前缀和减去之前某个位置的前缀和，就是这个子数组的前缀和。
因此，就可以转化为由当前前缀和减去k，就是某个要找的前缀和。如果字典中有，就说明这个前缀和出现过，就说明，字串存在。


# 11、滑动窗口最大值
![[Pasted image 20260202131218.png]]




```python
from collections import deque

class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        # 这里的 q 就是那个“精英名单”，里面存的是【下标】！
        # 为什么要存下标？因为我们要通过下标算它是不是过期了。
        q = deque() 
        result = []
        
        for i in range(len(nums)):
            # 1. 【踢弱者】
            # 只要队列里有人，而且队尾的那个人比新来的 nums[i] 小
            # 那队尾那个人就永远没机会出头了，直接淘汰
            while q and nums[q[-1]] < nums[i]:
                q.pop() 
            
            # 2. 【进新人】
            # 现在的 nums[i] 有资格排队了
            q.append(i)
            
            # 3. 【清过气】
            # 队头是最老的。如果队头的下标 等于 i - k
            # 说明它刚好滑出了窗口的左边界，必须退休
            # 举例：k=3，当前 i=3 (第4个数)，那 i-k=0 (第0个数) 必须走了
            if q[0] == i - k:
                q.popleft()
            
            # 4. 【记录结果】
            # 从第 k-1 个元素（也就是窗口刚满）开始，每次都记下队头
            if i >= k - 1:
                result.append(nums[q[0]])
                
        return result
```


在这个题目中，考到了滑动窗口的知识点。
一个个加入到窗口。如何加入？，如果这个值比窗口内的所有值都大。就说明窗口内的值不起作用了。就把这个放到队头。其余的全部失效。如果不比第一个值大，但是比其余的值大，在窗口内时，也可以加入，但是也要按照大小顺序。（因为第一个有可能不被挤出但是时效到了，被踢出来了，这个时候，排名第二的数字，就可以顺利继位，变成第一个）
但是这个值当队头是有时效性的。如果在不被后加的值踢出的情况下。其最多可以呆一个滑动窗口的大小。（也就是新来的值的下标减去窗口的大小为最大值得下标的时候，意味着它已经呆了一个滑动窗口的大小了，马上要出窗口了）

进队前铲除异己（比我弱的全踢），进队后听天由命（等着前面的大佬退休）。

知识补充：在进队的时候，会把自己小的值全部踢出。因为这个时候，比自己小的值，再也不会有当队头的机会了，所以会直接踢出。
可以理解为，这个队列 就是一个最大值，以及最大值候选队列。每个一进来，就跟最后一个比，比它小的踢出，逐个和前面的比，直到找到自己的位置（其后面的全部踢出）。然后等待队列前面的大佬退休。自己继位。


# 12、最小覆盖子串

![[Pasted image 20260202135053.png]]


```python
from collections import defaultdict, Counter

class Solution:

    def minWindow(self, s: str, t: str) -> str:

        # 1. 准备欠条

        need = Counter(t)

        # 购物车

        window = defaultdict(int)

        left, right = 0, 0

        # valid 表示：已经有几种字符的数量达标了？

        # 比如 t="ABC"，当 window 里 A有一了, B有一个, C有一个时，valid就是 3

        valid = 0

        # 记录最小覆盖子串的起始索引和长度

        start, min_len = 0, float('inf')

        while right < len(s):

            # --- 进货环节 ---

            c = s[right]

            right += 1

            # 如果进的货是欠条里需要的，就记账

            if c in need:

                window[c] += 1

                # 如果这个字符的数量刚好凑够了，valid + 1

                if window[c] == need[c]:

                    valid += 1

            # --- 断舍离环节 ---

            # 当所有债务都还清了 (valid == len(need))，尝试缩小窗口

            while valid == len(need):

                # 1. 更新最小长度

                if right - left < min_len:

                    start = left

                    min_len = right - left

                # 2. 准备扔掉左边的货

                d = s[left]

                left += 1

                # 如果扔掉的货是欠条里的

                if d in need:

                    # 如果扔之前刚好够，扔了就不够了，valid - 1

                    if window[d] == need[d]:

                        valid -= 1

                    window[d] -= 1

        return "" if min_len == float('inf') else s[start : start + min_len]
```


这个题目，仍然是滑动窗口的题目

这个题目维护了两个值，一个是当前窗口内的字母的分布。（每个字母出现的字数）。
一个是（valid 表示：已经有几种字符的数量达标了？，比如t为AAB，这个时候need={‘A’：2，‘B’：1}，如果valid等于2，就表示A有两个B有一个）。

不断地将字母加入到窗口中，并统计符合条件的字母个数。合格时，就记录当前的下表和长度。然后将前面的出窗口。这里分为三种情况。一种是不是需要的字母，直接出。一种是是需要的字母，但是其出去后，窗口内的字母仍满足条件（这个字母本来就有富裕，出去一个没问题）。还有一种是是需要的字母，并且其一出去，条件就不满足，知道新的这个字母进来。

每次找到符合条件的情况，都记录下表和长度，直到找到最短的一版。（一旦新版比旧版短，就更新）。


# 13、最大子数组和
![[Pasted image 20260203140331.png]]

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        # 1. 初始化
        # pre_sum 表示：必须包含当前这个数字的话，前面的最大和是多少
        # 刚开始就是第一个数
        pre_sum = nums[0]
        
        # max_sum 记录历史出现过的巅峰值
        max_sum = nums[0]
        
        # 2. 从第二个数开始遍历
        for i in range(1, len(nums)):
            # --- 核心决策 ---
            # 如果之前的累加和大于0，说明是“良性资产”，我们就加上它
            if pre_sum > 0:
                pre_sum = pre_sum + nums[i]
            # 如果之前的累加和小于等于0，说明是“不良资产”，只会拖累我
            # 那就不带它玩了，从当前数字 nums[i] 另起炉灶
            else:
                pre_sum = nums[i]
            
            # --- 记录巅峰 ---
            # 每次走一步，都要看看现在的身价是不是历史最高的
            max_sum = max(max_sum, pre_sum)
            
        return max_sum
```


类比：有点像选择的继承遗产，前面的和加起来是正的，对我有利，我就继承，加上现在这个数，一旦我发现前面的是负资产，那么我会抛弃前面的，只剩下现在的，继续向下走。


# 14、合并区间
![[Pasted image 20260203140940.png]]



```python
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        # 1. 没有任何区间，直接返回空
        if not intervals:
            return []
        
        # 2. 关键一步：排序！
        # 按照每个区间的【左端点】从小到大排序
        # x[0] 表示取每个子列表的第0个元素（开始时间）
        intervals.sort(key=lambda x: x[0])
        
        merged = []
        
        # 3. 遍历每一个区间
        for interval in intervals:
            # 如果列表为空，或者当前区间的开始时间 > 上一个区间的结束时间
            # 说明【没有重叠】，直接把当前区间加进去
            if not merged or interval[0] > merged[-1][1]:
                merged.append(interval)
            else:
                # 否则，说明【有重叠】
                # 我们不需要添加新区间，而是修改上一个区间的结束时间
                # 谁结束得晚，就听谁的（取最大值）
                merged[-1][1] = max(merged[-1][1], interval[1])
        
        return merged
```


人的常理，像时间即可。先对这个按开始时间进行排序，然后一个个加入合并后的区间，一旦合并后的区间最后一个和新的区间有重叠就取最大的一个。

# 15、轮转数组
![[Pasted image 20260203141755.png]]


```python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        # 1. 处理 k 比 n 大的情况
        k = k % n
        
        # 定义一个简单的翻转函数：把 nums 从 start 到 end 的部分翻转
        def reverse(start, end):
            while start < end:
                nums[start], nums[end] = nums[end], nums[start]
                start += 1
                end -= 1
        
        # 2. 魔法三步走
        # 第一步：整体翻转
        reverse(0, n - 1)
        
        # 第二步：翻转前 k 个 (索引 0 到 k-1)
        reverse(0, k - 1)
        
        # 第三步：翻转剩下的 (索引 k 到 n-1)
        reverse(k, n - 1)
```


```python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        n = len(nums)
        k = k % n
        # Python 的切片赋值是原地的（逻辑上），但右边会生成临时对象
        nums[:] = nums[-k:] + nums[:-k]

```

分为partA和partB。先整体反转，再分别部分反转即可。

第二种方法是使用了python的语言特性。



# 16、除了自身以外数组的乘积
![[Pasted image 20260203143243.png]]

```python

class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        n = len(nums)
        # 1. 初始化答案数组，先把所有位置填上 1
        answer = [1] * n
        
        # --- 第一轮：从左往右扫 ---
        # 计算每个位置【左边】所有数的乘积
        # answer[i] 应该等于 nums[0] * ... * nums[i-1]
        for i in range(1, n):
            # 现在的 answer[i] = 左边的 answer[i-1] * 左边的 nums[i-1]
            answer[i] = answer[i - 1] * nums[i - 1]
        
        # 此时 answer 数组里已经是左侧乘积了：[1, 1, 2, 6]
        
        # --- 第二轮：从右往左扫 ---
        # R 用来记录【右边】所有数的乘积，刚开始右边没数，所以是 1
        R = 1
        
        # 倒着遍历：n-1, n-2, ... 0
        for i in range(n - 1, -1, -1):
            # 对于索引 i：
            # 最终结果 = (左边的积 answer[i]) * (右边的积 R)
            answer[i] = answer[i] * R
            
            # 更新 R：把当前的 nums[i] 乘进 R 里，给下一个位置用
            R = R * nums[i]
            
        return answer
```


先算出左边值的乘积再算出右边的值的乘积。然后左右相乘，就是所有值的乘积 



# 17、缺失的第一个正数
![[Pasted image 20260203144147.png]]


```python
class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        n = len(nums)
        
        # --- 第一步：萝卜归位 ---
        for i in range(n):
            # 这是一个 while 循环，因为换回来的新数字可能还是需要换位置
            # 条件解释：
            # 1. 1 <= nums[i] <= n: 数字必须在有效范围内（负数和超级大的数直接忽略）
            # 2. nums[nums[i] - 1] != nums[i]: 目标位置上已经有正确的数字了吗？
            #    如果没有（比如两个3，第一个3归位了，第二个3就不用换了），才交换。
            #    这一步非常关键，防止死循环！
            while 1 <= nums[i] <= n and nums[nums[i] - 1] != nums[i]:
                # 目标索引
                target_index = nums[i] - 1
                # 交换 (把 nums[i] 放到它该去的地方)
                nums[i], nums[target_index] = nums[target_index], nums[i]
                
        # --- 第二步：查岗 ---
        for i in range(n):
            # 如果索引 i 上的数字不是 i + 1
            if nums[i] != i + 1:
                return i + 1
                
        # 如果所有人都对号入座了（比如 [1, 2, 3]），那缺失的就是下一个
        return n + 1
```


利用这个下标。让这个数字回到它应该在的地方去。
然后再去查，看看哪一个不匹配。不匹配的返回下标值加一。


# 18、矩阵置零
![[Pasted image 20260204132530.png]]


```python
class Solution:

    def setZeroes(self, matrix: List[List[int]]) -> None:

        """

        Do not return anything, modify matrix in-place instead.

        """

        m = len(matrix)

        n = len(matrix[0])

        # 1. 定义一个标志位，专门记录【第一列】是否有 0

        col0_flag = False

        # 2. 第一遍扫描：只做【标记】

        for i in range(m):

            # 如果第一列本身就有 0，先把 col0_flag 立起来

            if matrix[i][0] == 0:

                col0_flag = True

            # 从第二列开始看（因为第一列的逻辑被 col0_flag 接管了）

            for j in range(1, n):

                if matrix[i][j] == 0:

                    # 把 0 投影到【行头】和【列头】

                    matrix[i][0] = 0

                    matrix[0][j] = 0

        # 3. 第二遍扫描：根据标记【置零】

        # 注意：我们要倒着遍历，或者避开第一行第一列

        # 最安全的方法是：先处理子矩阵 (1..m, 1..n)

        for i in range(1, m):

            for j in range(1, n):

                # 只要行头或列头有一个是 0，自己就得变 0

                if matrix[i][0] == 0 or matrix[0][j] == 0:

                    matrix[i][j] = 0

        # 4. 最后处理【第一行】

        # 如果 matrix[0][0] 是 0，说明第一行原本有 0（或者被投影了）

        if matrix[0][0] == 0:

            for j in range(n):

                matrix[0][j] = 0

        # 5. 最后处理【第一列】

        # 依据是我们最开始存的 col0_flag

        if col0_flag:

            for i in range(m):

                matrix[i][0] = 0
```



先把第一行和第一列作为备忘录，检查到这一行要置零以后，将这一行列的头置为0。然后再检查整体，把第一行列为0的行列全部置零。（可以说是作为一个置零的标记）
但是第一行跟第一列的头都为（0，0）不能复用，外置一个变量，作为这一列是否置零的标志，行是否置零用（0，0）位置的信息




# 19、螺旋矩阵


![[Pasted image 20260204134116.png]]




```python
class Solution:

    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:

        if not matrix:

            return []

        # 1. 设定四个边界

        top, bottom = 0, len(matrix) - 1

        left, right = 0, len(matrix[0]) - 1

        res = []

        # 2. 只要墙没撞上，就一直转

        while top <= bottom and left <= right:

            # A. 向右走 (上边界)

            for i in range(left, right + 1):

                res.append(matrix[top][i])

            # 走完这一行，上边界下移

            top += 1

            # B. 向下走 (右边界)

            for i in range(top, bottom + 1):

                res.append(matrix[i][right])

            # 走完这一列，右边界左移

            right -= 1

            # --- 关键检查 ---

            # 因为 top 和 right 刚刚变了，有可能墙已经撞上了

            # 如果不检查，对于单行或单列的矩阵，下面的向左/向上操作会重复读取！

            if top > bottom or left > right:

                break

            # C. 向左走 (下边界)

            for i in range(right, left - 1, -1):

                res.append(matrix[bottom][i])

            # 走完这一行，下边界上移

            bottom -= 1

            # D. 向上走 (左边界)

            for i in range(bottom, top - 1, -1):

                res.append(matrix[i][left])

            # 走完这一列，左边界右移

            left += 1

        return res
```

假设贪吃蛇，设定4面墙，维护好四个边界变量即可。


# 20、旋转图像

![[Pasted image 20260204134613.png]]



```python
class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.
        """
        n = len(matrix)
        
        # 1. 第一步：转置 (Transpose)
        # 沿着主对角线互换元素
        for i in range(n):
            # 注意：j 从 i 开始遍历，只遍历对角线右上方（或者左下方）
            # 如果 j 从 0 开始，你刚换过去，下次又被换回来了，等于没动
            for j in range(i, n):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
        
        # 2. 第二步：左右翻转 (Reverse each row)
        # Python 有内置的 reverse 方法，直接对每一行操作即可
        for row in matrix:
            row.reverse()
            
        # 如果不能用 row.reverse()，也可以手动写个双指针交换：
        # for i in range(n):
        #     left, right = 0, n - 1
        #     while left < right:
        #         matrix[i][left], matrix[i][right] = matrix[i][right], matrix[i][left]
        #         left += 1
        #         right -= 1
```


旋转90度可以作为先转置再左右反转即可得到。

# 21、搜索二维矩阵II
![[Pasted image 20260205131347.png]]


```python
class Solution:

    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:

        if not matrix:

            return False

        m = len(matrix)

        n = len(matrix[0])

        # 1. 站在右上角

        # row: 第 0 行

        # col: 最后一列

        row = 0

        col = n - 1

        # 2. 只要没走出边界，就一直找

        while row < m and col >= 0:

            current_val = matrix[row][col]

            if current_val == target:

                # 找到了

                return True

            elif current_val > target:

                # 当前值太大 -> 这一列没戏了 -> 往左走

                col -= 1

            else:

                # 当前值太小 -> 这一行没戏了 -> 往下走

                row += 1

        # 走投无路也没找到

        return False

```


在这个题目中，相当于是站在右上角看世界。右上角，左边全是比他小的，下面全是比他大的。
要寻找的值比它（这个位置的）值大就向下，小就向左即可。


# 22、相交链表

![[Pasted image 20260205131956.png]]



```python
class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> ListNode:
        # 1. 边界检查：如果有一个为空，肯定不相交
        if not headA or not headB:
            return None
        
        # 2. 定义两个指针
        pA, pB = headA, headB
        
        # 3. 只要没相遇，就一直走
        # 即使不相交，最后 pA 和 pB 都会同时变成 None，循环也会结束 (None == None)
        while pA != pB:
            # pA 走法：如果走到头了(None)，就跳到 headB；否则往前走一步
            pA = headB if pA is None else pA.next
            
            # pB 走法：如果走到头了(None)，就跳到 headA；否则往前走一步
            pB = headA if pB is None else pB.next
            
        # 4. 相遇点就是交点（或者都是 None）
        return pA
```

- **指针 A 的路程**：先走 A (`a+c`)，再走 B (`b`) $\rightarrow$ 总共走到交点也就是 `a + c + b`。
    
- **指针 B 的路程**：先走 B (`b+c`)，再走 A (`a`) $\rightarrow$ 总共走到交点也就是 `b + c + a`。
    

**你看！ `a + c + b` 等于 `b + c + a`！**

只要它俩这样走，就一定会**在交点处同时相遇**。


# 23、反转链表
![[Pasted image 20260205132847.png]]





```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        # 1. 准备两个指针
        prev = None
        curr = head
        
        while curr:
            # 2. 关键步骤：先存下后面的路，防止断链
            temp = curr.next
            
            # 3. 修改指向：让当前节点指回前一个
            curr.next = prev
            
            # 4. 指针整体后移
            prev = curr
            curr = temp
            
        # 5. 最后 prev 会停在新的头节点上
        return prev
```


维护了两个指针，一个是当前指针，一个是当前指针的前一个指针。首先用temp记录当前节点的下一个节点，（第一步、因为要修改当前节点的下一个节点，先保存记录下来，直接修改就断链了）。然后让当前指针的下一个节点指向前一个节点。然后prev和curr一起后移动一个。


还可以用递归的方法。
```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        # 1. 递归终止条件（Base Case）
        # 如果链表为空，或者只有一个节点，那反转后还是它自己
        if not head or not head.next:
            return head
        
        # 2. 递（Going deep）：一直走到最后一个节点
        # new_head 会一直传递回来，它是反转后的新头（也就是原来的尾巴）
        new_head = self.reverseList(head.next)
        
        # 3. 归（Coming back）：在回溯过程中修改指向
        # 现在的 head 是倒数第二个节点，head.next 是最后一个节点
        # 我们要让 最后一个节点 指向 倒数第二个节点
        head.next.next = head
        
        # 4. 断开原来的连接，防止形成环 (1->2->1)
        head.next = None
        
        return new_head
```

这个暂时看不懂，（**以后再看**）



# 24、回文链表
![[Pasted image 20260205134958.png]]

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        if not head or not head.next:
            return True
            
        # 1. 快慢指针找中点
        slow, fast = head, head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            
        # 此时 slow 就是后半段的起点
        
        # 2. 反转后半段链表 (直接复用反转链表的逻辑)
        # 我们定义一个 helper 或者直接写在里面都可以
        prev = None
        curr = slow
        while curr:
            temp = curr.next
            curr.next = prev
            prev = curr
            curr = temp
            
        # 反转结束后，prev 就是后半段的新头（原来的尾巴）
        
        # 3. 前后半段逐个比对
        left, right = head, prev
        while right: # 只要右边没走完（因为右边通常比左边短或相等）
            if left.val != right.val:
                return False
            left = left.next
            right = right.next
            
        return True
```

使用快慢指针找中点。然后使用迭代法（上一题），将后半部分翻转。最后逐个比对。

# 25、环形链表
![[Pasted image 20260205135650.png]]






```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        # 1. 边界条件：如果没人或者只有一个人，肯定成不了环
        if not head or not head.next:
            return False
            
        # 2. 定义快慢指针
        slow = head
        fast = head
        
        # 3. 只要快指针还能跑（没到终点），就一直跑
        # 为什么是 fast.next？因为 fast 一次跑两步，所以要保证下下步也能走
        while fast and fast.next:
            slow = slow.next      # 慢走一步
            fast = fast.next.next # 快走两步
            
            # 4. 检查是否相遇（套圈了）
            if slow == fast:
                return True
                
        # 5. 如果循环结束了，说明 fast 冲过了终点（遇到 None）
        # 那就是没有环
        return False
```

只要有环，一个一次走两步，一个一次走一步。终有一时会套圈。

# 26、环形链表II
![[Pasted image 20260205140446.png]]


```python
class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        slow, fast = head, head
        
        # --- 第一阶段：判断是否有环 ---
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            
            # 如果相遇了，说明有环
            if slow == fast:
                
                # --- 第二阶段：找入口 ---
                # 1. 让其中一个指针回到起点
                p = head
                # 2. 另一个指针留在相遇点
                q = slow
                
                # 3. 两个人都一步一步走
                while p != q:
                    p = p.next
                    q = q.next
                
                # 4. 再次相遇点就是入口
                return p
        
        # 如果跑出来了，说明没环
        return None
```


假设：

- **L**：从头节点到环入口的距离（我们要求的）。
    
- **C**：环的长度。
    
- **X**：从环入口到相遇点 M 的距离。
    

**当它们相遇时：**

- **慢指针 (`slow`) 走了**：$L + X$
    
- **快指针 (`fast`) 走了**：$L + X + n \cdot C$ （它多跑了 $n$ 圈）
    

因为快指针速度是慢指针的 2 倍，所以路程也是 2 倍：

$$2 \times (L + X) = L + X + n \cdot C$$

化简一下：

$$L + X = n \cdot C$$

$$L = n \cdot C - X$$

**这就很有意思了：**

$$L = (n-1) \cdot C + (C - X)$$

- $L$ 是头到入口的距离。
    
- $C - X$ 是什么？是 **从相遇点 M 继续往前走回到入口的距离**。
    
- 这意味着：**“从头走到入口的距离”** 刚好等于 **“从相遇点走到入口的距离（可能多绕几圈）”**。
    

所以，一个从头走，一个从相遇点走，速度一样，必然会在入口撞见！




# 27、合并两个有序链表
![[Pasted image 20260209124909.png]]




```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        # 1. 创建哨兵节点 (Dummy Head)
        # 它的值是多少不重要，因为它最后会被丢弃
        dummy = ListNode(-1)
        
        # curr 指针用来在生成的新链表上移动
        curr = dummy
        
        # 2. 只要两个链表都有值，就开始比对
        while list1 and list2:
            if list1.val <= list2.val:
                curr.next = list1      # 谁小就把谁接过来
                list1 = list1.next     # 被选中的那个指针往后移
            else:
                curr.next = list2
                list2 = list2.next
            
            # 新链表的指针也要往后移，准备接下一个
            curr = curr.next
            
        # 3. 收尾工作
        # 循环结束后，肯定有一个链表先空了，另一个可能还有剩
        # 因为剩下的肯定是有序的，直接接在后面就行
        curr.next = list1 if list1 else list2
        
        # 4. 返回哨兵节点的下一个，那才是真正的头
        return dummy.next
```


正常比较大小，放在后面即可。




# 28、两数相加
**![[Pasted image 20260209125055.png]]**



```python
# Definition for singly-linked list.

# class ListNode:

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

  

class Solution:

    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:

        # 1. 哨兵节点，老朋友了

        dummy = ListNode(0)

        curr = dummy

        # 进位初始化为 0

        carry = 0

        # 2. 只要 l1 没走完，或者 l2 没走完，或者 还有进位没处理

        # 只要满足一个，就得继续算

        while l1 or l2 or carry:

            # 如果 l1 没了，就当成 0

            v1 = l1.val if l1 else 0

            # 如果 l2 没了，就当成 0

            v2 = l2.val if l2 else 0

            # 当前这一位的和

            total = v1 + v2 + carry

            # 计算新的进位 (比如 15 // 10 = 1)

            carry = total // 10

            # 计算当前位应该留下的数字 (比如 15 % 10 = 5)

            curr.next = ListNode(total % 10)

            # 指针移动

            curr = curr.next

            if l1: l1 = l1.next

            if l2: l2 = l2.next

        return dummy.next
```


正常的从个位到最后一位进行计算。




# 29、删除链表的倒数第N个结点


![[Pasted image 20260209125709.png]]




```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        # 1. 创建哨兵节点，指向 head
        dummy = ListNode(0, head)
        
        # 2. 初始化双指针，都指向 dummy
        slow = dummy
        fast = dummy
        
        # 3. 让 fast 先走 n + 1 步
        # 为什么要多走一步？因为我们要让 slow 停在被删除节点的【前一个】
        for _ in range(n + 1):
            fast = fast.next
            
        # 4. 两个指针同时移动，直到 fast 走到尽头 (None)
        while fast:
            slow = slow.next
            fast = fast.next
            
        # 5. 此时 slow 就在被删除节点的前一个位置
        # 执行删除操作
        slow.next = slow.next.next
        
        # 6. 返回哨兵节点的下一个（真正的头节点）
        return dummy.next

```
这个使用了快慢指针。正常来讲我们只知道链表的头。删除倒数第N个。我们可能需要知道这个链表的长度。
使用快慢指针，可以先让快指针走N+1步，然后两个指针一起走。快指针指向空，说明慢指针到了删除结点的前一个结点。就可以将节点删除。



什么使用使用快慢指针、
1、判断链表是否有环。
2、寻找环的入口。
3、寻找中点
4、寻找倒数第N个节点



|**使用场景**|**快慢指针的表现形式**|**核心目的**|**典型题目 (LeetCode)**|
|---|---|---|---|
|**环检测**|速度比 $2:1$|确认是否存在循环逻辑|141 (环形链表)|
|**找中点**|速度比 $2:1$|用于分治或对称性判断|234 (回文链表)|
|**找倒数第 N**|距离差为 $N$|解决单向链表无法回溯的问题|19 (删除倒数第 N)|
|**数组去重/修改**|逻辑速度差异（读写分离）|在 $O(1)$ 空间内原地修改|26 (删除有序数组重复项)|

# 30、两两交换链表中的节点。
![[Pasted image 20260209130227.png]]



```python
# Definition for singly-linked list.

# class ListNode:

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

  

class Solution:

    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:

        # 1. 创建哨兵节点，简化头部的操作

        dummy = ListNode(0, head)

        # prev 指针，永远指向“待交换对”的前一个节点

        prev = dummy

        # 2. 只要后面还有两个节点（能凑成一对），就继续交换

        while prev.next and prev.next.next:

            # 定义这一对节点

            curr = prev.next       # 第一个节点 (比如 1)

            next_node = curr.next  # 第二个节点 (比如 2)

            # --- 开始三步交换 ---

            # 步骤 1: 前驱指向第二个

            # dummy -> 2

            prev.next = next_node

            # 步骤 2: 第一个指向第三个（防止链表断开）

            # 1 -> 3 (3 是 next_node.next)

            curr.next = next_node.next

            # 步骤 3: 第二个指向第一个

            # 2 -> 1

            next_node.next = curr

            # --- 交换结束 ---

            # 3. 指针后移

            # 此时 pair 变成了 2 -> 1，prev 应该移动到 1 的位置

            # 准备处理下一对 (3, 4)

            prev = curr

        return dummy.next

```


在这个题目中，需要保证结点交换的顺序，如果顺序不对，可能会丢失后续的结点。
需要哨兵结点，要不还得需要单独判断是不是头结点。还得单独处理。

# 31、K个一组翻转链表


![[Pasted image 20260209131221.png]]



```python
# Definition for singly-linked list.

# class ListNode:

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

  

class Solution:

    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:

        dummy = ListNode(0, head)

        groupPrev = dummy

        while True:

            # 1. 找到这一组的结尾（第 k 个节点）

            kth = self.getKth(groupPrev, k)

            # 如果凑不够 k 个，说明剩下的不用翻转了，直接结束

            if not kth:

                break

            # 2. 记录下一组的开头，防止断链

            groupNext = kth.next

            # 3.截断这一组，方便 reverse 函数处理

            # 这一组的范围是：groupPrev.next ... kth

            # 我们先把 kth.next 切断，让它变成一个独立的链表

            prev, curr = kth.next, groupPrev.next # 这里有个小技巧，让 reverse 里的 prev 初始指向 groupNext，可以省去最后一步连接

            # 但为了逻辑清晰，我们还是用标准的切断法：

            start = groupPrev.next # 这一组的头（反转后会变成尾）

            kth.next = None        # 切断

            # 4. 反转这一组

            self.reverse(start)

            # 5. 缝合

            # 反转后，kth 变成了头，start 变成了尾

            groupPrev.next = kth   # 上一组的尾巴 -> 这一组的新头

            start.next = groupNext # 这一组的新尾巴 -> 下一组的头

            # 6. 移动指针，准备下一轮

            # groupPrev 应该移动到这一组的尾巴（也就是 start）

            groupPrev = start

        return dummy.next

  

    # 辅助函数：找第 k 个节点

    def getKth(self, curr, k):

        while curr and k > 0:

            curr = curr.next

            k -= 1

        return curr

    # 辅助函数：标准反转链表 (LeetCode 206)

    def reverse(self, head):

        prev = None

        curr = head

        while curr:

            temp = curr.next

            curr.next = prev

            prev = curr

            curr = temp

        return prev
```


这个题目需要反复敲熟练，因为比较乱，有两个函数，一个是对放进去的链表进行翻转，一个是计算K个的函数。
找出需要反转的k个。然后将其变为独立的链，然乎翻转，翻转后，再和之前的链表连上，



# 32、随机链表的复制

![[Pasted image 20260209133101.png]]



方法一：
```python
"""
# Definition for a Node.
class Node:
    def __init__(self, x: int, next: 'Node' = None, random: 'Node' = None):
        self.val = int(x)
        self.next = next
        self.random = random
"""

class Solution:
    def copyRandomList(self, head: 'Optional[Node]') -> 'Optional[Node]':
        if not head:
            return None
        
        # 1. 创建哈希表，存 {旧节点 : 新节点}
        old_to_new = {}
        
        curr = head
        # 第一轮：只创建节点，不连接
        while curr:
            old_to_new[curr] = Node(curr.val)
            curr = curr.next
            
        curr = head
        # 第二轮：根据旧节点的指向，连接新节点
        while curr:
            # 新节点的 next 指向 旧节点next 的分身
            if curr.next:
                old_to_new[curr].next = old_to_new[curr.next]
            
            # 新节点的 random 指向 旧节点random 的分身
            if curr.random:
                old_to_new[curr].random = old_to_new[curr.random]
                
            curr = curr.next
            
        return old_to_new[head]

```
这是hash表法，先建立结点。但是不连接。（不给next和random赋值）。只给val赋值，这个时候，字典内是{原节点 : 新节点}。但是原结点中的next和random的值还没有给新结点。然后第二轮就是将复制的结点链接起来。


方法二、拼接法
```python
class Solution:
    def copyRandomList(self, head: 'Optional[Node]') -> 'Optional[Node]':
        if not head:
            return None
            
        # 1. 复制节点，穿插在原链表中
        curr = head
        while curr:
            new_node = Node(curr.val, curr.next) # 新节点的 next 指向原节点的下一个
            curr.next = new_node                 # 原节点的 next 指向新节点
            curr = new_node.next                 # 跳到下一个原节点
            
        # 此时链表变成了：A -> A' -> B -> B'
        
        # 2. 处理 random 指针
        curr = head
        while curr:
            if curr.random:
                # A' 的 random 就是 A.random 的下一个 (也就是 A.random 的分身)
                curr.next.random = curr.random.next
            curr = curr.next.next # 跳过分身，去下一个原节点
            
        # 3. 拆分链表 (还原原链表，提取新链表)
        curr = head
        copy_head = head.next # 新链表的头
        
        while curr:
            copy = curr.next
            curr.next = copy.next # 原链表接回原链表
            if copy.next:
                copy.next = copy.next.next # 新链表接回新链表
            
            curr = curr.next # 移动指针
            
        return copy_head

```

先顺序复制一遍,此时都是   原节点-》此结点复制-》下一个原结点-》下一个原结点的复制。
然后再将结点拆分即可。



# 33、排序链表

![[Pasted image 20260209172526.png]]


```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        # 1. Base Case: 如果链表为空或只有一个节点，直接返回（已经有序）
        if not head or not head.next:
            return head
        
        # 2. 找中点 (快慢指针)
        slow, fast = head, head.next 
        # 注意：fast 从 head.next 开始，是为了让 slow 停在中点偏左的位置
        # 这样才能正确把链表分成两半（比如 2个节点时，slow停在第1个）
        
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            
        # 3. 断开链表
        mid = slow.next
        slow.next = None # 切断！
        
        # 4. 递归排序左右两半
        left_sorted = self.sortList(head)
        right_sorted = self.sortList(mid)
        
        # 5. 合并两个有序链表 (直接套用 LeetCode 21 的逻辑)
        return self.merge(left_sorted, right_sorted)
    
    # 辅助函数：合并两个有序链表
    def merge(self, l1, l2):
        dummy = ListNode(0)
        curr = dummy
        while l1 and l2:
            if l1.val < l2.val:
                curr.next = l1
                l1 = l1.next
            else:
                curr.next = l2
                l2 = l2.next
            curr = curr.next
        curr.next = l1 if l1 else l2
        return dummy.next

```


用递归，快慢指针找中点，以及前面的合并两个有序链表。



# 34、合并K个升序链表

![[Pasted image 20260303154637.png]]


```python
import heapq

from typing import List, Optional

  

# Definition for singly-linked list.

# class ListNode:

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

  

class Solution:

    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:

        # 1. 创建哨兵节点

        dummy = ListNode(0)

        curr = dummy

        # 创建小顶堆

        heap = []

        # 2. 初始化擂台：把所有链表的头节点放进堆里

        for i, node in enumerate(lists):

            if node:

                # 放入元组：(节点值, 链表索引, 节点对象)

                # 链表索引 i 的作用是防止两个节点值相同时，去比较 ListNode 对象引发报错

                heapq.heappush(heap, (node.val, i, node))

        # 3. 开始“打擂台”

        while heap:

            # 弹出当前堆中最小的节点

            val, i, node = heapq.heappop(heap)

            # 把最小的节点接到结果链表上

            curr.next = node

            curr = curr.next

            # 如果这个赢家节点还有下一个兄弟，让它的兄弟上擂台

            if node.next:

                heapq.heappush(heap, (node.next.val, i, node.next))

        return dummy.next
```


K个链表中，已经按升序排序过了。用一个优先堆。先把所有链表中的第一个放进堆栈中，按值比较。
然后出栈第一个。（每入栈一个，都会排序，将最小的放到第一个）。接着加入出栈结点的下一个结点。不断循环，直到排序完毕。


# 35、LRU缓存
![[Pasted image 20260303155007.png]]



```python
class DlinkNoede:

    def __init__(self,key = 0,value = 0):

        self.key = key

        self.value = value

        self.next = None

        self.pre = None

  
  

class LRUCache:

  

    def __init__(self, capacity: int):

        self.head = DlinkNoede()

        self.tail = DlinkNoede()

        self.head.next = self.tail

        self.tail.pre = self.head

        self.size = 0

        self.cache =dict()

        self.capacity = capacity

  

    def get(self, key: int) -> int:

        if key in self.cache:

            node = self.cache[key]

            self.movetohead(node)

            return node.value

            #并将其移动到头

        else:

            return -1          

  

    def put(self, key: int, value: int) -> None:

        if key not in self.cache:

            node = DlinkNoede(key,value)

            self.cache[key] = node

            self.size += 1

            self.addToHead(node)

  

            if self.size > self.capacity:      

                node = self.removetail()

                self.cache.pop(node.key)

                self.size -= 1

  

        else:

            node = self.cache[key]

            node.value = value

            self.movetohead(node)

  
  
  
  

    def addToHead(self,node):

        node.next = self.head.next

        node.pre = self.head

        self.head.next.pre = node

        self.head.next = node

  

    def removenode(self,node):

        node.next.pre = node.pre

        node.pre.next = node.next

    def movetohead(self,node):

        self.removenode(node)

        self.addToHead(node)

    def removetail(self):

        node = self.tail.pre

        self.removenode(node)

        return node

```



# 36、二叉树的中序遍历
![[Pasted image 20260303155617.png]]



递归法

```python

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def inorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        res = []
        
        def traverse(node):
            if not node:
                return
            
            # 1. 深入左子树
            traverse(node.left)
            # 2. 记录当前节点的值 (中)
            res.append(node.val)
            # 3. 深入右子树
            traverse(node.right)
            
        traverse(root)
        return res
```


迭代法

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def inorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        res = []
        stack = []
        curr = root
        
        # 只要当前节点不为空，或者栈里还有节点没处理，就继续循环
        while curr or stack:
            # 1. 只要有左孩子，就一路向左，全部压栈
            if curr:
                stack.append(curr)
                curr = curr.left
            # 2. 走到尽头了，弹出栈顶元素处理，并转向右边
            else:
                curr = stack.pop()
                res.append(curr.val) # 中
                curr = curr.right    # 右
                
        return res
```


# 37、二叉树的最大深度
![[Pasted image 20260304194454.png]]




```python
from typing import Optional

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        # 1. 递归终止条件 (Base Case)：如果节点为空，深度为 0
        if not root:
            return 0
        
        # 2. 深入左子树，求左子树的最大深度
        left_depth = self.maxDepth(root.left)
        
        # 3. 深入右子树，求右子树的最大深度
        right_depth = self.maxDepth(root.right)
        
        # 4. 合并结果：取左右子树深度的最大值，加上当前节点的深度 (1)
        return max(left_depth, right_depth) + 1
```


最大深度等于左边的最大深度和右边的最大深度两者取最大然后+1.进行递归即可求出整个树的高度

```python
from typing import Optional
from collections import deque

class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
            
        # 使用 deque 作为队列，支持 O(1) 的两端操作
        queue = deque([root])
        depth = 0
        
        while queue:
            # 记录当前层的节点数量
            level_size = len(queue)
            
            # 遍历当前层的所有节点
            for _ in range(level_size):
                # 从队首取出一个节点
                node = queue.popleft()
                
                # 如果有左孩子，放入队尾，准备下一层的遍历
                if node.left:
                    queue.append(node.left)
                # 如果有右孩子，放入队尾
                if node.right:
                    queue.append(node.right)
            
            # 当前层遍历完毕，深度加 1
            depth += 1
            
        return depth
```

这是广度优先算法。一层一层的将所有结点加进去。

# 38、翻转二叉树

![[Pasted image 20260304200201.png]]



```python
from typing import Optional

# 题目提供的二叉树节点定义
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        # 1. 递归终止条件：节点为空
        if not root:
            return None
        
        # 2. 核心操作：交换当前节点的左右子树
        # Python 支持这种优雅的多重赋值语法，不需要额外的 temp 变量
        root.left, root.right = root.right, root.left
        
        # 3. 递归：继续翻转下一层的左右子树
        self.invertTree(root.left)
        self.invertTree(root.right)
        
        # 4. 返回当前节点
        return root

```


通过递归翻转。

为什么最后会返回root。主要是为了代码评测。



```python
from typing import Optional
from collections import deque

# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        # 如果是空树，直接返回
        if not root:
            return None
            
        # 使用 collections.deque 作为队列，性能更佳
        queue = deque([root])
        
        while queue:
            # 从队首弹出一个节点
            current = queue.popleft()
            
            # 交换当前节点的左右孩子
            current.left, current.right = current.right, current.left
            
            # 如果存在左孩子，加入队列等待后续处理
            if current.left:
                queue.append(current.left)
            # 如果存在右孩子，加入队列等待后续处理
            if current.right:
                queue.append(current.right)
                
        # 遍历完所有节点后，返回原来的根节点
        return root
```


利用队列来翻转。


# 39、对称二叉树
![[Pasted image 20260304200833.png]]




```python
from typing import Optional

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        # 如果是空树，天然是对称的
        if not root:
            return True
            
        # 核心：判断左子树和右子树是否镜像
        return self.isMirror(root.left, root.right)
        
    def isMirror(self, left: Optional[TreeNode], right: Optional[TreeNode]) -> bool:
        # 1. 递归终止条件
        # 如果两个节点都为空，说明对称（到了叶子节点的下一层）
        if not left and not right:
            return True
        # 如果只有一个为空，说明不对称（结构不同）
        if not left or not right:
            return False
        # 如果都不为空，但值不相等，说明不对称
        if left.val != right.val:
            return False
            
        # 2. 核心逻辑：当前节点比对成功，继续往下一层比对
        # 左侧的左子树 vs 右侧的右子树  AND  左侧的右子树 vs 右侧的左子树
        return self.isMirror(left.left, right.right) and self.isMirror(left.right, right.left)

```


利用递归的判断。


```python
from typing import Optional
from collections import deque

# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        if not root:
            return True
            
        # 使用队列来保存需要比对的节点对
        queue = deque([(root.left, root.right)])
        
        while queue:
            # 每次弹出一对节点
            left, right = queue.popleft()
            
            # 1. 如果两个都为空，继续比对队列里的下一对
            if not left and not right:
                continue
                
            # 2. 如果结构不对称（一个空一个不空），或者值不相等，直接判定为非对称
            if not left or not right or left.val != right.val:
                return False
                
            # 3. 将下一层的节点按“镜像顺序”配对入队
            # 外侧配外侧
            queue.append((left.left, right.right))
            # 内侧配内侧
            queue.append((left.right, right.left))
            
        # 如果队列全部查完都没有发现不对称，则说明是轴对称的
        return True
```




# 40、二叉树的直径

![[Pasted image 20260304201134.png]]




```python
from typing import Optional

  

# 题目提供的二叉树节点定义

class TreeNode:

    def __init__(self, val=0, left=None, right=None):

        self.val = val

        self.left = left

        self.right = right

  

class Solution:

    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:

        # 初始化全局最大直径为 0

        self.max_diameter = 0

        # 辅助函数：计算二叉树的最大深度

        def depth(node: Optional[TreeNode]) -> int:

            # 终止条件：空节点深度为 0

            if not node:

                return 0

            # 后序遍历：先深入获取左右子树的最大深度

            left_depth = depth(node.left)

            right_depth = depth(node.right)

            # 核心逻辑：当前节点的直径是左右子树深度之和

            # 实时更新全局最大直径

            self.max_diameter = max(self.max_diameter, left_depth + right_depth)

            # 返回当前节点的最大深度（左右子树较深的一个 + 1）

            return max(left_depth, right_depth) + 1

        # 触发递归计算

        depth(root)

        return self.max_diameter

```


等于左右子树深度之和就是最大直径。


# 41、二叉树的层序遍历
![[Pasted image 20260304201715.png]]

```python

from typing import Optional, List
from collections import deque

# 题目提供的二叉树节点定义
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        # 如果是空树，直接返回空数组
        if not root:
            return []
            
        res = []
        # 初始化队列，并将根节点放入
        queue = deque([root])
        
        # 只要队列不为空，就说明还有层需要处理
        while queue:
            # 1. 拍快照：获取当前层的节点总数
            level_size = len(queue)
            # 用于存放当前层所有节点值的临时数组
            level_vals = []
            
            # 2. 遍历当前层的所有节点
            for _ in range(level_size):
                # 从队首取出一个节点
                node = queue.popleft()
                # 记录该节点的值
                level_vals.append(node.val)
                
                # 3. 将下一层的节点按从左到右的顺序入队
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            
            # 4. 当前层遍历完毕，将这一层的值数组存入总结果中
            res.append(level_vals)
            
        return res


```


采用层序遍历。




# 42、将有序数组转换为二叉搜索树

![[Pasted image 20260305214118.png]]


1. **是一棵二叉搜索树**：左子树所有节点的值 $<$ 根节点的值 $<$ 右子树所有节点的值。
    
2. **必须是高度平衡的**：每个节点的左右两个子树的高度差的绝对值不超过 1。


```python
from typing import List, Optional

  

# 题目提供的二叉树节点定义

# class TreeNode:

#     def __init__(self, val=0, left=None, right=None):

#         self.val = val

#         self.left = left

#         self.right = right

  

class Solution:

    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:

        # 辅助函数：通过边界指针递归构建 BST

        def helper(left: int, right: int) -> Optional[TreeNode]:

            # 1. 递归终止条件：当左边界越过右边界，说明当前范围内没有元素了

            if left > right:

                return None

            # 2. 找中点 (向下取整，对应数组长度为偶数时取左侧偏中的元素)

            # 使用 left + (right - left) // 2 可以有效防止数值溢出（虽然在 Python 中整数不会溢出，但这是好习惯）

            mid = left + (right - left) // 2

            # 3. 将中点作为当前的根节点

            root = TreeNode(nums[mid])

            # 4. 递归构建左右子树

            root.left = helper(left, mid - 1)

            root.right = helper(mid + 1, right)

            return root

        # 触发递归，初始边界为整个数组的头和尾

        return helper(0, len(nums) - 1)
```

# 43、验证二叉搜索树
![[Pasted image 20260418121307.png]]




递归上下界法
```python
# Definition for a binary tree node.

# class TreeNode:

#     def __init__(self, val=0, left=None, right=None):

#         self.val = val

#         self.left = left

#         self.right = right

class Solution:

    def isValidBST(self, root: Optional[TreeNode]) -> bool:

        def dfs(node, lower, upper):

            # 空树是合法 BST

            if not node:

                return True

            # 当前节点必须在合法范围内

            if not (lower < node.val < upper):

                return False

            # 左子树所有节点必须小于 node.val

            # 右子树所有节点必须大于 node.val

            return dfs(node.left, lower, node.val) and dfs(node.right, node.val, upper)

        return dfs(root, float("-inf"), float("inf"))
```

中序遍历法
```python
  

# class Solution:

#     def isValidBST(self, root: Optional[TreeNode]) -> bool:

#         prev = float("inf")

#         def inorder(node):

#             nonlocal prev

#             if not node:

#                 return True

#             # 先检查左子树

#             if not inorder(node.right):

#                 return False

#             # 当前节点必须大于前一个节点

#             if node.val >= prev:

#                 return False

#             prev = node.val

#             # 再检查右子树

#             return inorder(node.left)

#         return inorder(root)

class Solution:

    def isValidBST(self, root: Optional[TreeNode]) -> bool:

        prev = float("-inf")

        def inorder(node):

            nonlocal prev

            if not node:

                return True

            # 先检查左子树

            if not inorder(node.left):

                return False

            # 当前节点必须大于前一个节点

            if node.val <= prev:

                return False

            prev = node.val

            # 再检查右子树

            return inorder(node.right)

        return inorder(root)
```

# 44、二叉搜索树中第K小的元素

![[Pasted image 20260422144618.png]]

中序遍历加数组

```python
class Solution:

    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:

        arr = []

  

        def inorder(node):

            if not node:

                return

            inorder(node.left)

            arr.append(node.val)

            inorder(node.right)

  

        inorder(root)

        return arr[k - 1]
```


递归法：
```python
class Solution:
    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:
        self.k = k
        self.ans = -1

        def inorder(node):
            if not node or self.ans != -1:
                return
            
            inorder(node.left)

            self.k -= 1
            if self.k == 0:
                self.ans = node.val
                return

            inorder(node.right)

        inorder(root)
        return self.ans

```


模板：
```python
def inorder(node):
    if not node:
        return
    inorder(node.left)
    # 处理当前节点
    inorder(node.right)
    
def inorder(node):
    if not node:
        return

    inorder(node.left)

    k -= 1
    if k == 0:
        ans = node.val
        return

    inorder(node.right)
    
```