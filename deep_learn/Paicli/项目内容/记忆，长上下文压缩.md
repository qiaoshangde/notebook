
1、项目设计了两套上下文压缩机制？

第一套是短期记忆，用Map-reduce摘要压缩算法，但是没有使用。只是压缩做了个摆设

第二种压缩是消息历史压缩，保留最近的三条用户消息（用户与agent的交流之间都保留）。三条之前的消息，直接发送给llm，让llm总结，进行压缩。

保留三条用户消息吗，主要是为了避免切断完整的用户与agent以及tool的qa。

由于第一套短期记忆压缩机制没有使用，没有作用，下面将一下第二套压缩机制

第二套压缩机制的实现：







2、Scope机制是什么？项目代码？


3、AST
RAG检索只对java代码进行了分块。


