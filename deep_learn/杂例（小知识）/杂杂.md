


浮动图像又称源图像（source image），固定图像又称参考图像（reference image）、模板图像（template image），形变场又称流场（flow field）、配准场（registration field）、转换（transformation）。






对于大小为[W, H]的二维图像来说，其位移场大小为[W, H, 2]；对于大小为[D, W, H]的三维图像来说，其位移场大小为[D, W, H, 3]。位移场表示每个体素/像素在各个方向（x，y，z轴）的位移。空间转换网络会根据位移场生成一个归一化后的采样网格，然后用该网络对图像进行采样，就得到了配准后的图像。

VoxelMorph在进行配准时通常是要求数据进行过预配准（通常是仿射配准），而没有经过预配准的图像通常配准效果较差，这可能是因为此时的位移较大，而模型对大位移的处理能力较差。

state-of-art   最先进的

PyTorch 的输入格式通常为 `(batch_size, channels, depth, height, width)
TensorFlow 的输入格式通常为 `(batch_size, depth, height, width, channels)`



- `glob.glob`：用于查找符合特定模式的文件路径。
    
- 参数：`os.path.join(args.train_dir, '*.nii.gz')`，即匹配 `args.train_dir` 目录下所有 `.nii.gz` 文件。
    
- 返回值：一个列表，包含所有匹配的文件路径。
    
- 例如，如果 `args.train_dir` 目录下有 `image1.nii.gz` 和 `image2.nii.gz`，则返回 `["/data/train/image1.nii.gz", "/data/train/image2.nii.gz"]`。

print("i: %d  loss: %f  sim: %f  grad: %f" % (i, loss.item(), sim_loss.item(), grad_loss.item()), flush=True)


flush` 参数的作用

 1. **输出缓冲区**

- 在 Python 中，`print` 函数的输出通常会先写入缓冲区，而不是直接显示到终端或日志文件中。
    
- 缓冲区满或程序结束时，缓冲区的内容才会被刷新（即显示到终端或写入文件）。
    
 2. **`flush=True` 的行为**

- 当 `flush=True` 时，`print` 函数会立即刷新输出缓冲区，确保内容立即显示到终端或写入文件。
    
- 如果没有设置 `flush=True`，输出可能会在缓冲区中停留一段时间，直到缓冲区满或程序结束。
    
 3. **`flush=False` 的行为**

- 当 `flush=False`（默认值）时，`print` 函数的输出会先写入缓冲区，不会立即显示。
### `flush` 参数的作用

#### 1. **输出缓冲区**

- 在 Python 中，`print` 函数的输出通常会先写入缓冲区，而不是直接显示到终端或日志文件中。
    
- 缓冲区满或程序结束时，缓冲区的内容才会被刷新（即显示到终端或写入文件）。
    

#### 2. **`flush=True` 的行为**

- 当 `flush=True` 时，`print` 函数会立即刷新输出缓冲区，确保内容立即显示到终端或写入文件。
    
- 如果没有设置 `flush=True`，输出可能会在缓冲区中停留一段时间，直到缓冲区满或程序结束。
    

#### 3. **`flush=False` 的行为**

- 当 `flush=False`（默认值）时，`print` 函数的输出会先写入缓冲区，不会立即显示。

什么是Checkpoint？ 在机器学习和深度学习中，checkpoint（检查点）是指在模型训练过程中保存的模型状态。这些检查点通常包括模型的参数（权重和偏置）、优化器状态和其他相关的训练信息。通过保存检查点，您可以在训练过程中定期保存模型的当前状态，以便在需要时恢复训练或用于模型评估和推理。


DL = Data.DataLoader(DS, batch_size=args.batch_size, shuffle=True, num_workers=0, drop_last=True)


- **`DS`**：数据集对象，通常是 `torch.utils.data.Dataset` 的实例。
    
- **`batch_size=args.batch_size`**：每个批次的大小。
    
- **`shuffle=True`**：是否在每个 epoch 开始时打乱数据顺序。
    
- **`num_workers=0`**：用于数据加载的子进程数量。`0` 表示在主进程中加载数据。
    
- **`drop_last=True`**：是否丢弃最后一个不完整的批次。